<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodel="clr-namespace:customer_app.ViewModels" xmlns:comp="clr-namespace:customer_app.Views.Components"
             x:Class="customer_app.Views.RegisterPage">
    <ContentPage.BindingContext>
        <viewmodel:RegisterViewModels/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <StackLayout Orientation="Vertical" HorizontalOptions="CenterAndExpand">
                <Label Text="Welcome!" FontSize="Title" Margin="0,10"/>
                <Label Text="Create Your Account" FontSize="Small"/>
            </StackLayout>
            <StackLayout>
                <Frame CornerRadius="15" Margin="25,10,25,10" HasShadow="True" HeightRequest="40" Padding="0">
                    <Entry Text="{Binding name }" Placeholder="Name"/>
                </Frame>
                <Frame CornerRadius="15" Margin="25,10,25,10" HasShadow="True" HeightRequest="40" Padding="0">
                    <Entry Text="{Binding email }" Placeholder="Email"/>
                </Frame>
                <Frame CornerRadius="15" Margin="25,10,25,10" HasShadow="True" HeightRequest="40" Padding="0">
                    <Entry Text="{Binding phone }" Placeholder="Phone"/>
                </Frame>
                <Frame CornerRadius="15" Margin="25,10,25,10" HasShadow="True" HeightRequest="40" Padding="0">
                    <Entry Text="{Binding password }" Placeholder="Password" IsPassword="True"/>
                </Frame>
                <Frame CornerRadius="15" Margin="25,10,25,10" HasShadow="True" HeightRequest="40" Padding="0">
                    <Entry Text="{Binding location }" Placeholder="Location"/>
                </Frame>
                <Frame HasShadow="True" Padding="0" WidthRequest="150" HorizontalOptions="Center" CornerRadius="15">

                </Frame>
                <comp:ButtonComponent NameButton="Sign Up" CommandParameter="{Binding SigUpCommad}" Padding="20"/>
               

            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>