<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:customer_app.ViewModels"
             xmlns:loca="clr-namespace:customer_app.Views"
             x:Class="customer_app.Views.SearchBarberPage">
    <ContentPage.BindingContext>
        <viewmodels:SearchBarberViewModels/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Frame Margin="30,25,30,0" CornerRadius="20" BorderColor="Gray" WidthRequest="60" HeightRequest="30">

                <SearchBar  Placeholder="Type here to search.."
                       WidthRequest="60" HeightRequest="30"
                       CancelButtonColor="Purple"
                       PlaceholderColor="black"
                       TextColor="Black"
                       TextTransform="Lowercase"
                       FontSize="Medium"
                       FontAttributes="Italic" />
            </Frame>
            <Frame Margin="20,10,20,10" CornerRadius="20">
                <StackLayout>
                    <ListView x:Name="SearchBarber" ItemTapped="OnItemSelected" ItemsSource="{Binding Salon}" HasUnevenRows="True" >
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <StackLayout>
                                        <Line Margin="0,5,0,5"/>
                                        <StackLayout Orientation="Horizontal">
                                            <StackLayout>
                                                <Frame CornerRadius="100" 
                                                   HeightRequest="50"
                                                   WidthRequest="50"
                                                   HorizontalOptions="Center"
                                                   Padding="0" 
                                                   IsClippedToBounds="True">
                                                    <Image Source="monkey.jpg" 
                                                       HorizontalOptions="Center"
                                                       VerticalOptions="Center" />
                                                </Frame> 
                                            </StackLayout>
                                            <StackLayout Orientation="Vertical" HorizontalOptions="StartAndExpand" >
                                                <Label Text="{Binding SalonName}" VerticalOptions="EndAndExpand" FontAttributes="Bold"/>
                                                <Label Text="{Binding Location}" VerticalOptions="CenterAndExpand"/>
                                                <StackLayout Orientation="Horizontal">
                                                    <Label Text="Open : "/>
                                                    <Label Text="{Binding StartTime}" VerticalOptions="StartAndExpand"/>
                                                    <Label Text="AM"/>
                                                    <Label Text="-" FontSize="15"/>
                                                    <Label Text="{Binding EndTime}" VerticalOptions="EndAndExpand"/>
                                                    <Label Text="PM"/>
                                                </StackLayout> 
                                            </StackLayout>
                                            <StackLayout VerticalOptions="CenterAndExpand">
                                                <ImageButton Source="" />
                                            </StackLayout>
                                        </StackLayout>
                                    </StackLayout>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackLayout>
            </Frame>
        </StackLayout>
    </ContentPage.Content>
   
</ContentPage>